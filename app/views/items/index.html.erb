<h1>PocketChange</h1>

<table>

<% @items.each do |item| %>
  <tr>
  
    <td><%= link_to image_tag(item.picture.url, size:"100x100"), item %>
        <br>
        <%= item.name %> 
        <br>
        $<%= item.price %> 
        
        <%= button_tag "Buy"%> 
    </td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_item_path %>
