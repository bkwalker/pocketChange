<table>

<div class="container">
   <ul class="row">
	<% @items.each do |item| %>
	  <tr>
	  
	    <li class="col-lg-2 col-md-2 col-sm-3 col-xs-4"><%= link_to image_tag(item.picture.url, size:"100x100"), item %>
		<br>
		<%= item.name %> 
		<br>
		$<%= item.price %> 
		
		<%= button_tag "Buy"%> 
	    </td>
	    
	  </tr>
	<% end %>
   </ul>
</div>
</table>

<br />

<%= link_to 'New Item', new_item_path %>
